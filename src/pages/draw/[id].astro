---
import { Toaster } from "@/components/ui/sonner";
import Layout from "@/layouts/Layout.astro";
import DrawByIdPage from "@/page-components/draw-pages/draw-by-id";

export const prerender = false;

// Alternative: If you want to keep it static, uncomment this instead of using prerender = false
// export async function getStaticPaths() {
//   // Return empty array since we want to generate pages on-demand
//   return [];
// }
---

<Layout>
	<DrawByIdPage client:only="react" />
	<Toaster richColors client:only="react" /> 
</Layout>